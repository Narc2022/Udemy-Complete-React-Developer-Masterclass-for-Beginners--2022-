import React from "react";

const CatComponent = (props) => {
  return (
    <h1>
      catName: {props.catName}eyeColor: {props.catName},age:{props.age}
    </h1>
  );
};

export default CatComponent;
